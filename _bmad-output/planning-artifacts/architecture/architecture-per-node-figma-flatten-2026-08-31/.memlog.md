---
scope: Per-source-node Design versus Image choices during Figma-to-React-Email import
purpose: Build substrate for Epic 5 implementation stories
altitude: feature
updated: 2026-08-31T20:17
---

- (decision) AD-1: Source-node render-mode overrides are chosen before AST generation and keyed by stable Figma nodeId; this prevents confusing source IDs with generated dotted AST paths.
- (decision) AD-2: A chosen image node is an atomic outermost subtree; selected descendants are normalized away, one 2x PNG is fetched, and AST traversal emits one Img without recursing.
- (decision) AD-3: Explicit user flatten choices override registry component linking and force primitives mode; automatic icon suggestions alone preserve current registry-link precedence.
- (decision) AD-4: The import UI exposes all eligible non-root containers and graphic nodes with searchable name/type/dimensions/text/ID; unchecked means Design and checked means Image.
- (decision) AD-5: Flatten choice is materialized into the generated AST Img and local upload URL; no Figma token, parsed source tree, or editor-only mode metadata is required in exported email.
- (assumption) Mobile-paired subtree override mapping is deferred because the requested example supplies one frame and desktop/mobile node IDs are not guaranteed to correspond.
- (decision) AD-6: Per-node selections address the desktop source tree only; forced desktop PNGs are shared across viewports and excluded from opportunistic mobile name/path replacement.
- (event) Reviewer gate corrections applied; deterministic lint passed with zero findings; spine finalized.
