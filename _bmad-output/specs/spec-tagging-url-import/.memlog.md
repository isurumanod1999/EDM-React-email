---
topic: Fast URL / tagging import for EDM React Email Tool
goal: Apply tagging-doc FINAL URL, labels, and alt text onto matching template targets with UI review and export fidelity
updated: 2026-08-09T22:20
---

- (event) bmad-spec create started; interactive; express distill from chat intent + docs/NEXT-PLANS.md + docs/CONFLUENCE + registry link fields; Book1.xlsx sample path cited but file contents unread this session (shell blocked)
- (direction by user) Add URLs, labels, alt text from tagging doc into the codebase/tool; FINAL URL must land on suitable component image/CTA/link/matching target; UI must view and check; values must appear in exported HTML
- (note) Sample sheet path: c:\Users\IsuruSenanayake\OneDrive - Maytech Holdings Pty Ltd\Desktop\Book1.xlsx — user states it has FINAL URL
- (capability) CAP-1 Import tagging document rows (FINAL URL + label + alt + match keys) into a working session for a template
- (capability) CAP-2 Propose mappings from tagging rows to linkable targets on the current template (images, CTAs, logos, text links, and other registry URL fields)
- (capability) CAP-3 Review, correct, confirm, or reject proposed mappings in the builder UI before apply
- (capability) CAP-4 Apply confirmed FINAL URL / label / alt onto the matched block props so they persist on the template document
- (capability) CAP-5 Show applied links in preview and ensure exported HTML contains the same href / alt / visible CTA text from those props
- (capability) CAP-6 Provide an in-tool checklist to click/verify each applied URL (desktop + mobile preview contexts)
- (constraint) Applied values must flow through existing registry block props (e.g. ctaUrl, buttonUrl, url, logoUrl, altText, logoAlt, buttonText/ctaText) so export/render reuse AD-6 — no parallel unlinked URL store that export ignores
- (constraint) User must be able to override auto-matches; silent wrong mapping is unacceptable for campaign QA
- (constraint) Feature stays inside current architecture baseline (Epic 1–2 ports/adapters; no F1–F4 unless requested)
- (constraint) Tagging import is in-tool for the campaign template under edit — not a separate offline script-only path as the primary UX
- (non-goal) Real inbox/email-client render QA (Gmail/Outlook/iOS) is a separate pending track
- (non-goal) Generating tracking/UTM URLs from scratch; sheet supplies FINAL URL
- (non-goal) Carousel, colour selector, hotspot component builds
- (non-goal) CRM Handlebars personalization tokens / Accelerator Gulp path changes
- (assumption) Tagging fields at minimum are Tracking/FINAL URL, Label, and Alt text per docs/NEXT-PLANS.md
- (assumption) "Label" may mean analytics link name and/or CTA visible text depending on target type — needs confirmation
- (assumption) xlsx (Excel) is the primary upload format for v1; csv may be acceptable as stretch
- (question) What exact columns exist on Book1.xlsx / production tagging sheets beyond FINAL URL (headers for label, alt, component name, section, desk/mob, order)?
- (question) What is the authoritative match key between a tagging row and a block target (row order, section name, Figma frame name, image filename, component type + index, manual pin)?
- (question) Does Label write analytics metadata only, visible CTA/button text, both, or vary by row type?
- (question) Must unmatched rows block export, warn only, or allow partial apply?
- (question) Is click-verify (CAP-6) required in the same sprint as apply+export, or can it follow immediately after?
- (decision) Spec slug = tagging-url-import; companion holds target-prop catalog and sheet mapping gaps
- (event) Pass 1 coherence self-validate: capabilities have intent+success; constraints bend design; non-goals present; success signal demonstrable; lean prose OK; open questions capture Excel unread gap
- (event) Pass 2 preservation self-validate: user FINAL URL / label / alt / UI check / export HTML claims landed; Confluence/NEXT-PLANS tagging claims landed; registry prop names in companion; Book1 path noted as unread source sample
- (direction by user) Upload the Excel tagging file only after the entire template is built; no need to add URLs while building components
- (decision) Tagging is a post-compose step: compose full template layout first (URLs optional/empty during build); then upload xlsx → map → review → apply → export/verify
- (constraint) Builder must not require FINAL URLs during component assembly / Figma import; empty or placeholder link props are acceptable until tagging apply
- (non-goal) Forcing URL entry in the property panel as part of the build-components flow
- (event) Spec updated for post-build Excel upload workflow; re-derived SPEC.md + companions
- (decision) Book1 confirms FINAL URL, URL Label, Alt Text; match key = URL Label; architecture spine finalized AD-12..AD-19
- (assumption) FINAL URL kept with CRM tokens; partial apply; no CTA text overwrite from URL Label
- (event) Architecture companion adopted on SPEC; open questions reduced to deferred product tightenings
